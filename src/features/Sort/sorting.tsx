import { FC } from 'react'

export const Sorting: FC = () => {
	return (
		<select>
			<option value={'sanchir'}>sanchir</option>
			<option value={'anton'}>anton</option>
			<option value={'batr'}>batr</option>
		</select>
	)
}
